meta {
  name: Flashcards - list for tag
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/tags/{{tagId}}/flashcards
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  tagId: 31
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("returns an array of flashcards", function () {
    const body = res.getBody();
    expect(body.success).to.equal(true);
    expect(body.data.tag_name).to.be.a("string");
    expect(body.data.flashcards).to.be.an("array");
  });
}

docs {
  Requires {{tagId}} — run "4-generate-flashcards" first.
}
